"""Все настройки и константы бенчмарка.

Пути собраны в bench/paths.py и считаются от корня репозитория, поэтому
скрипты работают из любой рабочей директории. Значения ниже -- умолчания;
почти все они переопределяются аргументами командной строки.
"""

import os

import cv2

from .paths import OUTPUT_DIR, example_path, model_path

# --- Имена моделей (файлы лежат в models/, скачиваются bench/download.py) ---
SEG_MODEL_NAME = "selfie_multiclass_256x256.tflite"
POSE_MODEL_NAME = "pose_landmarker_lite.task"
FACE_MODEL_NAME = "face_landmarker.task"
YOLO_MODEL_NAME = "yolo26n.pt"
YOLO_SEG_MODEL_NAME = "yolo26n-seg.pt"

# --- Пути к моделям и видео ---
SEG_MODEL_PATH = str(model_path(SEG_MODEL_NAME))
POSE_MODEL_PATH = str(model_path(POSE_MODEL_NAME))
FACE_MODEL_PATH = str(model_path(FACE_MODEL_NAME))
YOLO_MODEL_PATH = str(model_path(YOLO_MODEL_NAME))
YOLO_SEG_MODEL_PATH = str(model_path(YOLO_SEG_MODEL_NAME))
VIDEO_PATH = str(example_path("example.mp4"))

SHOW_CURR_MASK = False

# --- YOLO-кроп ---
USE_YOLO_CROP = False
CROP_PADDING = 0.1
YOLO_CONF = 0.3

# --- Включение моделей ---
ENABLE_SEGMENTATION = True
ENABLE_POSE = True
ENABLE_FACE = False

# --- Ограничение головы эллипсом ---
HEAD_ELLIPSE_RADIUS_U_COEF = 0.5  # полуось ВДОЛЬ плеч (полуширина головы), a = coef * S
HEAD_ELLIPSE_RADIUS_N_COEF = 0.8   # полуось К ГОЛОВЕ (полувысота, нос->макушка), b = coef * S
ENABLE_HEAD_CONSTRAINT = False      # применять ограничение к маске
DRAW_HEAD_ELLIPSE = False           # рисовать верхний полуэллипс на кадре

# --- Общие ---
SAVE_OUTPUT_VIDEO = True
OUTPUT_VIDEO_PATH = str(OUTPUT_DIR / "example_tracked.mp4")
SHOW_PREVIEW = False   # окно можно держать выключенным
WARMUP_FRAMES = 3
DRAW_POSE = True                # скелет поверх маски при отслеживании
DRAW_FACE_OVAL = False
DRAW_YOLO_BBOX = True
MAX_FRAMES = None               # None = всё видео; число = ограничить


# --- Категории сегментации ---
CATEGORIES = {0: "background", 1: "hair", 2: "body-skin",
              3: "face-skin", 4: "clothes", 5: "others"}
CATEGORY_COLORS = {
    0: (0, 0, 0), 1: (0, 165, 255), 2: (0, 255, 255),
    3: (255, 255, 0), 4: (128, 0, 128), 5: (0, 255, 0),
}

# --- Соединения скелета (упрощённо, без лица) ---
POSE_CONNECTIONS = [
    (11, 12), (11, 13), (13, 15), (12, 14), (14, 16),
    (11, 23), (12, 24), (23, 24),
    (23, 25), (25, 27), (24, 26), (26, 28),
]

# --- Овал лица (FACE_OVAL), по контуру ---
FACE_OVAL = [
    10, 338, 297, 332, 284, 251, 389, 356, 454, 323,
    361, 288, 397, 365, 379, 378, 400, 377, 152, 148,
    176, 149, 150, 136, 172, 58, 132, 93, 234, 127,
    162, 21, 54, 103, 67, 109,
]

# --- Индексы точек позы для круга головы ---
POSE_NOSE = 0
POSE_LEFT_SHOULDER = 11
POSE_RIGHT_SHOULDER = 12

# --- Тепловая карта уверенности (временно вместо цветовой классификации) ---
SHOW_CONFIDENCE_HEATMAP = True        # True = heatmap уверенности, False = цвета категорий
HEATMAP_COLORMAP = cv2.COLORMAP_JET   # JET, INFERNO, MAGMA, TURBO и др.
HEATMAP_STRENGTH = 0.8                # сила наложения heatmap (0..1)

# --- Модель stickman (stickman + rectangles) ---
DRAW_STICKMAN = True                    # рисовать модель тела
STICKMAN_HEAD_W_COEF = 0 #0.45             # k_hw: ширина головы = k_hw * S
STICKMAN_HEAD_H_COEF = 0 #0.60             # k_hh: высота головы = k_hh * S
STICKMAN_TORSO_SCALE = 0 #1.2              # масштаб торса относительно его центра (1.0 = без изменений)
# Опускание точек бёдер перпендикулярно линии торса 23-24 (в долях S)
STICKMAN_PALM_COEF = 0.2               # k_palm: толщина прямоугольника ладони = k_palm * S
STICKMAN_FOOT_COEF = 0.5915               # k_foot: толщина ступни = k_foot * S_hip(таз)
# Удлинение отрезка наружу от сустава: 1.0 = без удлинения, 1.3 = +30%
STICKMAN_TORSO_EXTEND_COEF = 0 #0.4
STICKMAN_PALM_EXTEND_COEF = 2         # отрезок ладони: 15 -> середина 19 и 17 (16 -> 20 и 18)
STICKMAN_FOOT_EXTEND_COEF = 1.15        # отрезок ступни 27-31 / 28-32
# Ступня строится выпуклым пятиугольником от нижней стороны голени
# (см. build_foot_poly). Ширина отрезка в точке продления обратно
# зависит от проекции лодыжка-пятка |27-29|: чем больше стопа развёрнута
# на камеру, тем короче эта проекция и тем шире видна стопа.
STICKMAN_FOOT_WIDTH_COEF = 6.0         # W = coef * S_hip / |27-29|
STICKMAN_FOOT_WIDTH_CAP_COEF = 0.6      # потолок W в долях S_hip
STICKMAN_LIMB_COEFS = {                 # руки: coef * S(плечи), ноги: coef * S_hip(таз)
    (11, 13): 0.36,   # левое плечо (верх руки)
    (13, 15): 0.33,   # левое предплечье
    (12, 14): 0.36,   # правое плечо (верх руки)
    (14, 16): 0.33,   # правое предплечье
    (23, 25): 1, #0.676,   # левое бедро
    (25, 27): 1, #0.6084,   # левая голень
    (24, 26): 1, #0.676,   # правое бедро
    (26, 28): 1, #0.6084,   # правая голень
}
# Предплечье короче плечевой кости в столько раз (или сильнее) -- рука
# смотрит в камеру, её длины на кадре нет. Тогда вместо вырожденной фигуры
# предплечья на локтевой стороне плеча строится квадрат.
STICKMAN_FOREARM_SHORT_RATIO = 4.0
STICKMAN_FALLBACK_TORSO_ANGLE = 75.0    # угол фолбэк-трапеции торса
STICKMAN_FALLBACK_LENGTH_FACTOR = 3.0   # глубина фолбэк-трапеции
STICKMAN_COLOR = (255, 100, 0)          # цвет модели (BGR)
STICKMAN_ALPHA = 0.5                    # прозрачность заполнения

# --- Калибровка stickman-модели по первому кадру ---
CALIBRATION_W_EXTRA_THRESHOLD = 0.05   # порог W_extra: 5% от ширины плеч S
CALIBRATION_EAR_EXTEND_COEF = 1.5      # макс. продление от ушей: 1.5 × |7-8|
CALIBRATION_SHOULDER_EXTEND_COEF = 1.5
# Рука, вытянутая вдоль линии плеч, ломает вытягивание верхних вершин торса:
# оно уходит по руке, и торс раздувается на всю её длину. Если угол при плече
# между отрезком плеч и плечевой костью больше порога, вместо вытягивания
# вдоль плеч из плеча пускается луч под CALIBRATION_SHOULDER_RAY_DEG к отрезку
# плеч -- наружу и вниз, к боку.
CALIBRATION_ARM_SPREAD_DEG = 150.0     # больше не используется: луч пускается всегда
CALIBRATION_SHOULDER_RAY_DEG = 135.0   # угол луча к отрезку плеч
CALIBRATION_HIP_EXTEND_COEF = 1.5      # макс. вытягивание отрезка бёдер (в долях S)
# Линия живота: параллельна линии плеч, на доле расстояния плечи -> торс.
# Выключатель общий для калибровки и отслеживания: True -- торс рисуется
# шестиугольником плечи-живот-торс, False -- прежним четырёхугольником
# плечи-торс (линия живота не считается и не рисуется).
CALIBRATION_BELLY_ENABLED = False
CALIBRATION_BELLY_COEF = 0.5     # доля перпендикулярного расстояния
CALIBRATION_BELLY_EXTEND_COEF = 1.5    # макс. вытягивание до границы маски (в долях S)
# Ширина конечности в каждой её точке: луч из точки перпендикулярно отрезку
# до границы маски. Единицы -- те же, что у STICKMAN_LIMB_COEFS: доли ширины
# плеч для рук, доли ширины таза для ног.
CALIBRATION_LIMB_RAY_COEF = 1.5            # макс. длина самого луча
CALIBRATION_LIMB_WIDTH_MIN_COEF = 0.02     # ниже -- замер вырожден, берём STICKMAN_LIMB_COEFS
# Запас к порогу «луч короткий»: луч считается коротким, пока он меньше
# половины номинальной ширины, увеличенной на LIMB_EXTEND_COEF.
LIMB_EXTEND_COEF = 0.4
# Шея: трапеция «низ головы -- линия плеч» поджимается к маске на этом числе
# уровней. Профиль меряется от осевой линии наружу, но не дальше самой
# трапеции, поэтому фигура может только сузиться, но не вылезти.
# Дораскрытие фигур рук и ног по маске. Замер лучами даёт ширину самой
# конечности, но одежда шире неё, и по краям остаётся непокрытая полоса.
# Если флаг включён, фигура пары, чья ширина получена лучами (хотя бы с одной
# из симметричных сторон), симметрично раздвигается в обе стороны, пока в
# очередной прибавленной полосе маски не меньше CALIBRATION_LIMB_GROW_MIN_FILL.
# Замеры вырожденного предплечья (вместо него строится квадрат, см.
# STICKMAN_FOREARM_SHORT_RATIO) в расчёт не идут и не раздвигаются.
CALIBRATION_LIMB_GROW_ENABLED = True
CALIBRATION_LIMB_GROW_STEP_COEF = 0.02   # шаг на сторону, доли масштаба пары
CALIBRATION_LIMB_GROW_MAX_COEF = 0.5     # предел суммарного раздвижения
CALIBRATION_LIMB_GROW_MIN_FILL = 0.75    # доля маски в прибавленной полосе
CALIBRATION_NECK_LEVELS = 9
# Боковая сторона шеи ведётся по границе ground truth маски: от верхнего ребра
# торса вверх, до входа в прямоугольник головы (или в овал лица, если тот
# распознан). Прямая сторона трапеции для этого не годится -- при наклонённой
# голове её нижнее ребро оказывается НИЖЕ верхнего ребра торса, трапеция
# вырождается в плоский клин поперёк груди, и плечи в фигуру не попадают.
CALIBRATION_NECK_TRACE_ENABLED = True
CALIBRATION_NECK_TRACE_MAX_COEF = 2.0    # предел длины обхода, доли ширины плеч
# Перед подъёмом сторона спускается по контуру, пока не войдёт в фигуру торса:
# вершина торса бывает за кадром, и ближайшая к ней точка контура оказывается
# выше верхнего ребра торса -- между шеей и торсом остаётся клин. Спуск должен
# быть коротким: если торс рядом не нашёлся, ход по контуру уходит вниз по
# руке, и фигура шеи распухает (на example.mp4 без предела -- в 4.6 раза).
CALIBRATION_NECK_DESCEND_COEF = 0.15     # предел спуска, доли ширины плеч
# Допуск у овала лица: обход останавливается, не доходя до него столько-то
# (доли ширины плеч). Овал идёт по самой линии челюсти, а силуэт маски там
# проходит вплотную СНАРУЖИ -- без допуска обход не засчитывает касание и
# уходит обходить всю голову. 0 -- требовать попадания строго внутрь.
CALIBRATION_NECK_OVAL_TOL_COEF = 0.03
# На время обхода шеи прямоугольник головы временно расширяется по своей
# ширине на столько. Обход останавливается о расширенный прямоугольник, то
# есть крайние точки шеи L0/R0 находятся чуть раньше -- не упираясь в самые
# волосы. Наружу расширение не идёт: ни в маску модели, ни в трекинг; на
# calibration_result.png оно рисуется отдельным цветом, для наглядности.
CALIBRATION_NECK_HEAD_WIDEN_COEF = 0.20
# Овал лица на то же время раздувается от своего центра. Он идёт по самой
# линии челюсти, а силуэт маски проходит там вплотную снаружи -- на мелком
# лице (на full065 овал 39x47 px) допуска не хватает, касание не
# засчитывается, и обход уходит вокруг головы. Раздутый овал ловит силуэт
# надёжнее. Наружу, как и расширение прямоугольника, не идёт.
CALIBRATION_NECK_OVAL_WIDEN_COEF = 0.60
CALIB_HEAD_WIDE_COLOR = (0, 128, 255)    # цвет временного расширения (BGR)
# Основание шеи. Нижние точки сторон стоят на разной высоте (обход
# останавливается там, где сторона вошла в фигуру торса), и полоса между их
# уровнями не покрыта ни шеей, ни торсом. Из нижней точки каждой стороны
# ведётся линия вдоль плеч в сторону противоположной стороны до границы маски;
# по четырём точкам строится четырёхугольник, он входит в маску модели.
CALIBRATION_NECK_BASE_ENABLED = True
CALIB_NECK_BASE_COLOR = (0, 200, 255)    # цвет основания шеи на картинке (BGR)
# Верх шеи. Точки L0/R0 стоят там, где обход вошёл в голову, а нижнее ребро
# прямоугольника головы проходит ниже -- полоса между ними не покрыта.
# Четырёхугольник строится по L0, R0 и двум точкам, где нижнее ребро головы
# выходит из маски (если конец ребра сам внутри маски, берётся он). Он входит
# в маску модели.
CALIBRATION_NECK_TOP_ENABLED = True
CALIB_NECK_TOP_COLOR = (255, 0, 200)     # цвет верха шеи на картинке (BGR)
# --- Подбор нижней границы головы, когда подбородок не найден ---
# Опускаем down_dist, максимизируя IoU фигуры (голова + шея) с маской
# в фиксированной полосе между верхним ребром торса и уровнем носа.
CALIBRATION_NECK_FIT_ENABLED = True     # включить подбор (иначе -- симметрично len_XN)
CALIBRATION_NECK_FIT_MIN_COEF = 0.15    # нижняя граница перебора (в долях len_XN)
CALIBRATION_NECK_FIT_MAX_COEF = 1.20    # верхняя граница перебора (в долях len_XN)
CALIBRATION_NECK_FIT_STEP_PX = 1.0      # шаг перебора, px
CALIBRATION_NECK_FIT_MARGIN_PX = 1.0    # запас над верхним ребром торса, px
CALIBRATION_NECK_FIT_DEBUG = False      # печатать всю кривую IoU(d)

CALIBRATION_LEG_COEFS = {              # дефолтные коэффициенты ног для вычитания
    (23, 25): 0.20,   # левое бедро
    (25, 27): 0.16,   # левая голень
    (24, 26): 0.20,   # правое бедро
    (26, 28): 0.16,   # правая голень
}

# --- Отслеживание stickman-модели ---
ENABLE_STICKMAN_TRACKING = True    # применять модель при отслеживании

# --- Компенсация ракурса головы -------------------------------------------
# Прямоугольник головы калибруется на одном кадре, а на видео голова
# поворачивается и наклоняется: в проекции меняются и высота, и ширина, а
# откалиброванные доли ширины плеч остаются прежними.
#
# Ракурс считывается с вектора нос(0) -> середина ушей(7-8). Нос вынесен
# вперёд от оси вращения головы, поэтому в проекции этот вектор растёт и
# поворачивается ровно тогда, когда меняется ракурс. Он раскладывается по
# осям прямоугольника (обе компоненты -- в долях ширины плеч):
#   a -- вдоль линии ушей  (признак поворота влево/вправо),
#   b -- вдоль оси головы  (признак наклона вперёд/назад).
# Работает разница с калибровкой:
#   da = |a| - |a_калибровки|,   db = b - b_калибровки
#
#   размер = (откалиброванная доля + K_a * da + K_b * db) * ширину плеч
#
# У поворота берётся МОДУЛЬ: влево и вправо голова укорачивается в проекции
# одинаково, знаковый член сделал бы модель несимметричной. Наклон знаковый:
# запрокинутая голова и опущенная -- разные случаи. Проверено на контроле
# (коэффициенты по половине кадров, оценка по другой): модуль даёт остаток
# 0.0084 против 0.0199 у знакового по ширине и 0.047 против 0.058 по высоте.
#
# Коэффициенты подобраны МНК по 61 эталонному прямоугольнику (маска
# InSPyReNet + подбородок face_landmarker) на кадрах example/my.mp4;
# см. tools_head_anchor/harvest_gt.py и fit_final.py.
HEAD_VIEW_COMP_ENABLED = True
HEAD_VIEW_COMP = {                 # сторона: (K_a, K_b)
    'right': (0.2425, 0.1164),
    'left':  (0.2425, 0.1164),
    'up':    (-0.0244, 0.8835),
    'down':  (0.2892, 1.1023),
}
HEAD_VIEW_COMP_MIN_COEF = 0.05     # пол размера в долях плеч: не схлопываться

# Куда calibrate_stickman.py кладёт результат калибровки
CALIBRATION_PARAMS_OUTPUT_PATH = str(OUTPUT_DIR / "calibration_params.json")
CALIBRATION_RESULT_IMAGE_PATH = str(OUTPUT_DIR / "calibration_result.png")
# Готовая калибровка для example.mp4, идущая вместе с репозиторием
CALIBRATION_PARAMS_EXAMPLE_PATH = str(example_path("calibration_params.json"))


def default_calibration_params_path():
    """Путь к параметрам калибровки, используемый при отслеживании.

    Своя калибровка из output/ имеет приоритет; если её ещё нет, берётся
    демо-калибровка из example/, чтобы track_stickman.py работал сразу
    после клонирования.
    """
    if os.path.exists(CALIBRATION_PARAMS_OUTPUT_PATH):
        return CALIBRATION_PARAMS_OUTPUT_PATH
    return CALIBRATION_PARAMS_EXAMPLE_PATH


CALIBRATION_PARAMS_PATH = CALIBRATION_PARAMS_OUTPUT_PATH  # обратная совместимость

# --- Скрипт калибровки (calibrate_stickman.py) ---
CALIB_YOLO_MODEL_NAME = "yolo26s.pt"          # детектор человека (точнее, чем n)
CALIB_POSE_MODEL_NAME = "pose_landmarker_full.task"  # точный скелет
CALIB_FACE_MODEL_NAME = "face_landmarker.task"       # подбородок
CALIB_YOLO_MODEL_PATH = str(model_path(CALIB_YOLO_MODEL_NAME))
CALIB_POSE_MODEL_PATH = str(model_path(CALIB_POSE_MODEL_NAME))
CALIB_FACE_MODEL_PATH = str(model_path(CALIB_FACE_MODEL_NAME))

CALIB_FRAME_INDEX = 5           # номер кадра для калибровки (1-based)
CALIB_YOLO_BBOX_PADDING = 0.10  # запас вокруг box-а: 10% от размера
CALIB_FACE_CHIN_INDEX = 152     # индекс подбородка (chin tip) в face_landmarker
# Повторный поиск лица, когда на кропе всего человека оно не нашлось: кроп
# берётся вокруг построенного квадрата головы, увеличенного в столько раз.
CALIB_HEAD_CROP_EXPAND = 1.15
CALIB_MASK_OVERLAY_ALPHA = 0.5  # прозрачность маски InSPyReNet на визуализации
# --- Прямоугольники конечностей на визуализации калибровки ---
DRAW_CALIB_LIMBS = True                # рисовать прямоугольники рук и ног
CALIB_ARM_COLOR = (255, 0, 255)        # цвет рук (BGR, пурпурный)
CALIB_ARM_TRI_COLOR = (255, 0, 255)    # цвет треугольников плеча (BGR, пурпурный)
CALIB_JOINT_TRI_COLOR = (0, 165, 255)  # цвет треугольников локтя и колена (BGR, оранжевый)
CALIB_FOREARM_SQUARE_COLOR = (0, 100, 255)  # цвет квадрата короткого предплечья (BGR)
CALIB_LEG_COLOR = (255, 255, 0)        # цвет ног (BGR, голубой)
CALIB_THIGH_RECT_COLOR = (0, 255, 128)  # цвет прямоугольников бёдер (BGR)

# Как строится верх ног. Целое число:
#   0 -- две фигуры (26-24-23) и (25-23-24): верхняя сторона идёт от вершины
#        торса к ПРОТИВОПОЛОЖНОЙ точке таза, фигуры перекрывают друг друга
#        в паху;
#   1 -- те же две фигуры, но верхняя сторона идёт к точке A, середине
#        отрезка 23-24: (26-24-A) и (25-23-A), сходятся в одной точке;
#   2 -- ОДНА фигура на обе ноги, шестиугольник: нижние вершины торса
#        (BL и BR) и ОБЕ откалиброванные вершины у каждого колена -- 26 и 25.
#        Пах закрывается целиком, без стыка; внутренние вершины колен входят
#        в контур, поэтому при разной высоте колен низ фигуры идёт по ним, а
#        не срезает их прямой.
STICKMAN_THIGH_QUAD_MODE = 2
# Выбирать режим автоматически при калибровке: строим все три варианта и
# берём тот, у которого выше IoU с ground truth маской ВНУТРИ шестиугольника
# (варианта 2) -- он и служит областью сравнения. Выбранный режим пишется в
# JSON и используется при отслеживании.
#
# Выбор не делается и берётся режим 0, если:
#   * в четырёхугольник по точкам 23-24-26-25 (таз и колени) попали фигуры
#     рук (предплечья, ладони) -- тогда маска в этой зоне не про ноги, и
#     сравнивать по ней нечего;
#   * фигуры конечностей 24-26 и 23-25 касаются друг друга -- ноги сведены,
#     просвета между ними нет, и вариант с одной фигурой ничем не отличается.
STICKMAN_THIGH_QUAD_AUTO = True

# Ног не видно (ни одного колена в кадре), а таз виден: вниз строится
# прямоугольник до полного выхода нижнего ребра за кадр. Верхнее ребро --
# НИЖНЕЕ РЕБРО откалиброванного торса (BL-BR): оно уже подогнано по маске и
# шире отрезка 23-24, который идёт по самим точкам таза. Если торса нет,
# берётся отрезок 23-24. Иначе ниже таза у модели нет ничего, хотя тело в
# кадре продолжается.
# Одно колено видно, второе нет: невидимое строится зеркально видимому
# относительно осевой линии откалиброванного торса (середина верхнего ребра --
# середина нижнего). Поза в такой ситуации либо теряет точку совсем, либо
# ставит её наугад, а ноги у человека симметричны относительно этой оси.
STICKMAN_MIRROR_MISSING_KNEE = True

STICKMAN_LEGS_BELOW_FRAME = True
CALIB_LEGS_BELOW_COLOR = (200, 0, 255)   # цвет этой фигуры на картинке (BGR)
CALIB_PALM_COLOR = (255, 255, 255)     # цвет ладоней (BGR, белый)
CALIB_FOOT_COLOR = (128, 128, 255)     # цвет ступней (BGR, светло-красный)
CALIB_BELLY_COLOR = (0, 0, 255)        # цвет линии живота (BGR, красный)
CALIB_SHOULDERS_BOTTOM_COLOR = (0, 0, 0)  # цвет фигур "плечи-низ" и нижней шеи (BGR, чёрный)
CALIB_HIP_TRI_COLOR = (0, 0, 0)        # цвет треугольников таза (BGR, чёрный)
CALIB_TORSO_TRI_COLOR = (0, 0, 128)    # цвет треугольников торс-нога (BGR, тёмно-красный)
CALIB_THIGH_QUAD_COLOR = (0, 0, 128)   # цвет четырёхугольников бёдер (BGR, тёмно-красный)
CALIB_UPPER_HULL_COLOR = (0, 128, 0)   # цвет многоугольника XABCDY (BGR, тёмно-зелёный)
CALIB_ARM_TOPS_COLOR = (0, 128, 0)     # цвет четырёхугольника ABCD (BGR, тёмно-зелёный)
# Отдельная отрисовка фигур при отслеживании. Все они входят в маску
# stickman, поэтому по умолчанию выключены -- иначе поверх маски ложился бы
# второй слой заливки и дубль её границы. True -- фигура рисуется своим
# цветом поверх маски.
DRAW_TRACKED_HEAD = False                            # рисовать голову поверх маски
DRAW_TRACKED_TORSO = False                           # рисовать торс поверх маски
DRAW_TRACKED_NECK = False                      # рисовать шею поверх маски
TRACKED_NECK_COLOR = (255, 100, 0)             # цвет шеи (BGR)
TRACKED_HEAD_COLOR = (255, 100, 0)                   # цвет головы (BGR)
TRACKED_TORSO_COLOR = (255, 100, 0)                  # цвет торса (BGR)
DRAW_TRACKED_PALMS = False                     # рисовать ладони поверх маски
DRAW_TRACKED_FEET = False                      # рисовать ступни поверх маски
TRACKED_PALM_COLOR = (255, 255, 255)           # цвет ладоней (BGR, белый)
TRACKED_FOOT_COLOR = (128, 128, 255)           # цвет ступней (BGR, светло-красный)
TRACKED_THICKNESS = 2                                # толщина контуров

# --- Отладка шеи при отслеживании -------------------------------------------
# Показать на итоговом видео, из чего собрана фигура шеи. Малиновым -- её
# собственные вершины по уровням: L0..Ln и R0..Rn, где 0 -- уровень у головы,
# n -- у торса. Жёлтым -- опорные точки, от которых они отсчитываются:
#   C        -- середина отрезка 11-12, начало системы координат профиля;
#   11, 12   -- плечи: задают ось u и масштаб S = |11-12|;
#   H3, H2   -- нижние углы прямоугольника головы (по нему нормаль профиля
#               разворачивается в сторону головы);
#   TL, TR   -- верхние вершины торса: при калибровке от них начинается обход
#               по маске, при отслеживании к ним пристыковывается низ шеи.
DRAW_NECK_POINTS = False
NECK_POINT_COLOR = (255, 0, 255)     # вершины фигуры шеи (BGR, малиновый)
NECK_REF_COLOR = (0, 255, 255)       # опорные точки (BGR, жёлтый)
NECK_POINT_RADIUS = 5
NECK_POINT_LABELS = True             # подписывать точки
